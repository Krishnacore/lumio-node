apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lumio-validator.serviceAccountName" . }}
  labels:
{{ include "lumio-validator.labels" . | nindent 4 }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lumio-validator.fullname" . }}-validator
  labels:
    {{- include "lumio-validator.labels" . | nindent 4 }}

---

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lumio-validator.fullname" . }}-fullnode
  labels:
    {{- include "lumio-validator.labels" . | nindent 4 }}

--- 

apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "lumio-validator.fullname" . }}-haproxy
  labels:
    {{- include "lumio-validator.labels" . | nindent 4 }}
